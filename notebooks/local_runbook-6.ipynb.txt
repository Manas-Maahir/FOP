{
 "cells": [
  {
   "cell_type": "markdown",
   "id": "35554d9c",
   "metadata": {},
   "source": [
    "# SymFormer / TBX11K — **Local Jupyter** Runbook (Core-Method PoC)\n",
    "\n",
    "A de-Colab'd version of `notebooks/colab_runbook.ipynb`. Runs on a plain local Jupyter kernel.\n",
    "Run the cells top-to-bottom.\n",
    "\n",
    "**What changed vs the Colab notebook**\n",
    "\n",
    "- **No `google.colab` / no `drive.mount`.** There is no Drive here — every path is a local\n",
    "  folder inside your workspace.\n",
    "- **No trash-quota dance.** The Colab version's whole \"weights are disposable because Drive\n",
    "  Trash eats your 15 GB\" logic only existed because a Drive mount turns deletes into 30-day\n",
    "  trash. Locally, deletes are just deletes. Nothing is auto-removed; the final cleanup cell is\n",
    "  **opt-in and dry-run by default**.\n",
    "- **Everything lives under the repo** in folders your `.gitignore` already ignores\n",
    "  (`data/`, `tbx11k_tb512/`, `work_dirs/`, `tb_runs/`, `*.pth`, `*.zip`), so no build artifact\n",
    "  or dataset can be committed by accident.\n",
    "- **CPU fallback is real but slow.** The training scripts auto-select CUDA when available and\n",
    "  fall back to CPU otherwise. On CPU, run the **smoke test** (Phase 4) to prove the pipeline\n",
    "  end-to-end and skip the 24-epoch runs unless you have a GPU.\n",
    "\n",
    "Stack is **torch + torchvision only** (same as Colab) — no mmcv/mmdetection.\n",
    "See `paper.md`, `CLAUDE.md`, `limitations.md` in the repo for method + target numbers."
   ]
  },
  {
   "cell_type": "markdown",
   "id": "bbf07060",
   "metadata": {},
   "source": [
    "## Phase 0 — local layout\n",
    "\n",
    "| What | Where (local) | Notes |\n",
    "|---|---|---|\n",
    "| code / docs | the repo root (`PROJECT`) | this notebook's working dir |\n",
    "| raw TBX11K | `data/TBX11K` | tens of GB; downloaded once, then reused |\n",
    "| compact TB-only 512² set | `tbx11k_tb512` | ~250–400 MB; the only thing you *need* to keep |\n",
    "| checkpoints | `work_dirs/<run>` | script keeps only the latest by default (`--max-keep-ckpts 1`) |\n",
    "| logs (`train_log.jsonl` / `eval_log.jsonl`) — **the results** | `tb_runs/<run>` | a few KB; these are what your claims rest on |\n",
    "\n",
    "`--drive-sync` in the tools just means \"also copy the tiny logs to this dir\". Locally we point it\n",
    "at `tb_runs/` so the log-collection cells keep working — nothing Google is involved."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "cd96bf81",
   "metadata": {},
   "outputs": [],
   "source": [
    "# --- locate the repo, then define all workspace paths under it -----------------------------\n",
    "import os, sys, subprocess, shutil\n",
    "from pathlib import Path\n",
    "\n",
    "REPO_URL = 'https://github.com/Manas-Maahir/FOP.git'\n",
    "\n",
    "# If we're already inside the repo (tools/ present), use it. Otherwise clone it next to us.\n",
    "if Path('tools/tv_train.py').exists():\n",
    "    PROJECT = Path.cwd()\n",
    "else:\n",
    "    target = Path.cwd() / 'FOP'\n",
    "    if not (target / 'tools' / 'tv_train.py').exists():\n",
    "        print('Cloning repo ->', target)\n",
    "        subprocess.run(['git', 'clone', REPO_URL, str(target)], check=True)\n",
    "    else:\n",
    "        print('Repo already present, pulling latest ...')\n",
    "        subprocess.run(['git', '-C', str(target), 'pull', '--ff-only'])\n",
    "    os.chdir(target)\n",
    "    PROJECT = target\n",
    "\n",
    "# All workspace dirs live INSIDE the repo, using names already in .gitignore.\n",
    "DATA_RAW = PROJECT / 'data' / 'TBX11K'   # raw dataset (large)\n",
    "DATA_512 = PROJECT / 'tbx11k_tb512'      # compact TB-only 512 COCO set (built in Phase 3)\n",
    "WORK     = PROJECT / 'work_dirs'         # checkpoints\n",
    "RUNS     = PROJECT / 'tb_runs'           # logs = the results\n",
    "\n",
    "# Safety rail: we only ever delete things under WORKSPACE, never outside it.\n",
    "WORKSPACE = PROJECT.resolve()\n",
    "def _guard(p):\n",
    "    p = Path(p).resolve()\n",
    "    if WORKSPACE not in p.parents and p != WORKSPACE:\n",
    "        raise RuntimeError(f'refusing to touch a path outside the workspace: {p}')\n",
    "    return p\n",
    "\n",
    "for d in (DATA_RAW.parent, WORK, RUNS):\n",
    "    d.mkdir(parents=True, exist_ok=True)\n",
    "\n",
    "print('project        :', PROJECT)\n",
    "print('raw dataset    :', DATA_RAW)\n",
    "print('compact set    :', DATA_512)\n",
    "print('checkpoints    :', WORK)\n",
    "print('logs (results) :', RUNS)"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "13f3e551",
   "metadata": {},
   "source": [
    "## Phase 1 — environment\n",
    "\n",
    "Unlike Colab, torch/torchvision/pycocotools are **not** preinstalled locally, so we install what's\n",
    "missing. The cell below installs the CPU/default build. **If you have an NVIDIA GPU**, install the\n",
    "matching CUDA build of torch first from <https://pytorch.org/get-started/locally/> and re-run —\n",
    "the check will then see CUDA.\n",
    "\n",
    "> Windows note: `pycocotools` needs a C build toolchain. If the wheel fails, install\n",
    "> \"Microsoft C++ Build Tools\", or `pip install pycocotools-windows`."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "17aa6504",
   "metadata": {},
   "outputs": [],
   "source": [
    "# Base deps (safe everywhere) + the torch stack (only if missing).\n",
    "import importlib, subprocess, sys\n",
    "\n",
    "def ensure(pkgs):\n",
    "    missing = []\n",
    "    for mod, pip_name in pkgs:\n",
    "        try:\n",
    "            importlib.import_module(mod)\n",
    "        except ImportError:\n",
    "            missing.append(pip_name)\n",
    "    if missing:\n",
    "        print('installing:', missing)\n",
    "        subprocess.run([sys.executable, '-m', 'pip', 'install', '-q', *missing], check=True)\n",
    "\n",
    "ensure([('numpy','numpy>=1.24'), ('PIL','pillow>=9'), ('cv2','opencv-python>=4.6'),\n",
    "        ('matplotlib','matplotlib>=3.6'), ('gdown','gdown')])\n",
    "\n",
    "# torch + torchvision: install default build only if torch is absent.\n",
    "try:\n",
    "    import torch\n",
    "except ImportError:\n",
    "    print('torch not found — installing default (CPU) build; see markdown above for GPU builds')\n",
    "    subprocess.run([sys.executable, '-m', 'pip', 'install', '-q', 'torch', 'torchvision'], check=True)\n",
    "    import torch\n",
    "import torchvision\n",
    "\n",
    "try:\n",
    "    import pycocotools\n",
    "except ImportError:\n",
    "    subprocess.run([sys.executable, '-m', 'pip', 'install', '-q', 'pycocotools'], check=True)\n",
    "    import pycocotools\n",
    "\n",
    "dev = 'cuda' if torch.cuda.is_available() else 'cpu'\n",
    "print('python     ', sys.version.split()[0])\n",
    "print('torch      ', torch.__version__, '| cuda:', torch.cuda.is_available(),\n",
    "      '|', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU only')\n",
    "print('torchvision', torchvision.__version__)\n",
    "print('device     ', dev)\n",
    "if dev == 'cpu':\n",
    "    print('\\n[!] No CUDA. The unit tests, data prep and the Phase 4 SMOKE TEST will run fine,')\n",
    "    print('    but the full 24-epoch runs (Phases 5/7/8) will be very slow — skip them on CPU.')\n",
    "\n",
    "(PROJECT / 'requirements.lock.txt').write_text(\n",
    "    f\"python=={sys.version.split()[0]}\\ntorch=={torch.__version__}\\n\"\n",
    "    f\"torchvision=={torchvision.__version__}\\n\")\n",
    "print('\\nwrote requirements.lock.txt (record these in results.md)')"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "0bd1aed9",
   "metadata": {},
   "outputs": [],
   "source": [
    "# Optional: confirm a GPU if you expect one. Harmless if nvidia-smi isn't installed.\n",
    "import shutil, subprocess\n",
    "if shutil.which('nvidia-smi'):\n",
    "    subprocess.run(['nvidia-smi'])\n",
    "else:\n",
    "    print('nvidia-smi not found — assuming no NVIDIA GPU (CPU run).')"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "dbab4906",
   "metadata": {},
   "source": [
    "## Phase 6 (early) — unit tests\n",
    "These gate the model code and need **no data** — run them before downloading tens of GB."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "ef271a52",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, sys\n",
    "for t in ['tests/test_numpy_ref.py', 'tests/test_sas.py', 'tests/test_tv_model.py']:\n",
    "    print('='*70); print('RUN', t); print('='*70)\n",
    "    subprocess.run([sys.executable, t])"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "a2224c03",
   "metadata": {},
   "source": [
    "## Phase 2 — download the raw dataset (local)\n",
    "\n",
    "Goes to `data/TBX11K`. Skipped automatically if the compact set already exists, or if the raw\n",
    "data is already present.\n",
    "\n",
    "If `gdown` fails (Google throttles large public files), download the archive manually from\n",
    "<https://github.com/yun-liu/Tuberculosis>, put the `.zip` at `data/tbx11k.zip`, and re-run — the\n",
    "cell will find and extract it."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "e6939b67",
   "metadata": {},
   "outputs": [],
   "source": [
    "import os, glob, zipfile, subprocess, sys\n",
    "from pathlib import Path\n",
    "\n",
    "TBX11K_GDRIVE_ID = '1r-oNYTPiPCOUzSjChjCIYTdkjBTugqxR'  # official file id (yun-liu/Tuberculosis)\n",
    "ZIP = DATA_RAW.parent / 'tbx11k.zip'\n",
    "\n",
    "def compact_ready(p):\n",
    "    p = Path(p)\n",
    "    return ((p/'annotations'/'tb_train_agnostic.json').is_file()\n",
    "            and (p/'annotations'/'tb_val_agnostic.json').is_file()\n",
    "            and (p/'images'/'train').is_dir()\n",
    "            and any((p/'images'/'train').iterdir()))\n",
    "\n",
    "# clean stale/empty leftovers from a previous failed run (workspace-guarded)\n",
    "if DATA_512.is_dir() and not compact_ready(DATA_512):\n",
    "    print('removing stale/incomplete compact dir:', DATA_512)\n",
    "    shutil.rmtree(_guard(DATA_512), ignore_errors=True)\n",
    "if DATA_RAW.is_dir() and not any(DATA_RAW.iterdir()):\n",
    "    _guard(DATA_RAW).rmdir()\n",
    "\n",
    "have_raw = DATA_RAW.is_dir() and any(DATA_RAW.iterdir())\n",
    "\n",
    "if compact_ready(DATA_512):\n",
    "    print('compact dataset already built ->', DATA_512, '\\nSkip to Phase 4.')\n",
    "elif have_raw:\n",
    "    print('raw dataset already present ->', DATA_RAW, '\\nSkip to the Phase 2 gate below.')\n",
    "else:\n",
    "    if not ZIP.is_file():\n",
    "        print('Downloading TBX11K (tens of GB) via gdown ...')\n",
    "        subprocess.run([sys.executable, '-m', 'gdown', TBX11K_GDRIVE_ID, '-O', str(ZIP)])\n",
    "    # validate: a real archive is large; a tiny 'zip' is Google's quota/scan HTML page\n",
    "    if ZIP.is_file() and ZIP.stat().st_size > 10_000_000 and zipfile.is_zipfile(ZIP):\n",
    "        print(f'got archive: {ZIP.stat().st_size/1e9:.1f} GB — extracting ...')\n",
    "        with zipfile.ZipFile(ZIP) as z:\n",
    "            z.extractall(DATA_RAW.parent)\n",
    "        ZIP.unlink()\n",
    "    else:\n",
    "        if ZIP.is_file():\n",
    "            print('!! downloaded file is not a valid zip (likely Google\\'s quota/scan HTML page).')\n",
    "            print('   first bytes:', ZIP.read_bytes()[:200])\n",
    "            ZIP.unlink()\n",
    "        print('\\n!! No usable archive. Google throttles large public files. Options:')\n",
    "        print('   1. Open the link in a browser, copy it to YOUR Drive, download it, then place')\n",
    "        print('      the zip at', ZIP)\n",
    "        print('      https://drive.google.com/file/d/' + TBX11K_GDRIVE_ID + '/view')\n",
    "        print('   2. Or download from https://github.com/yun-liu/Tuberculosis and put the zip there.')\n",
    "        print('   Then re-run this cell.')\n",
    "\n",
    "print('\\nContents of', DATA_RAW.parent, ':')\n",
    "for d in sorted(DATA_RAW.parent.glob('*')):\n",
    "    try:\n",
    "        n = len(list(d.iterdir())) if d.is_dir() else '-'\n",
    "    except OSError:\n",
    "        n = '?'\n",
    "    print(f'    {d}   ({n} entries)')"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "d2dc695b",
   "metadata": {},
   "source": [
    "## Phase 2 GATE — inspect the actual layout\n",
    "The official README documents download links but not the folder structure, so we discover it.\n",
    "Expect ~1,200 TB images (train ~600 / val ~200 per the paper). If the listing above shows a\n",
    "different top-level folder, point `DATA_RAW` at it before running this."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "7755a010",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, sys\n",
    "subprocess.run([sys.executable, 'tools/prepare_tbx11k.py', '--inspect', '--src', str(DATA_RAW)])"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "20642923",
   "metadata": {},
   "source": [
    "## Phase 3 — build the compact TB-only 512² COCO set\n",
    "Layout is auto-discovered; override with `--xml-dir` / `--train-list` / `--val-list` if the gate\n",
    "above showed something unexpected. This compact set is the one artifact worth keeping."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "6a2fddfa",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, sys\n",
    "# verify the prep logic on synthetic data first\n",
    "subprocess.run([sys.executable, 'tools/prepare_tbx11k.py', '--selftest'])\n",
    "# build the real compact set\n",
    "subprocess.run([sys.executable, 'tools/prepare_tbx11k.py',\n",
    "                '--src', str(DATA_RAW), '--dst', str(DATA_512),\n",
    "                '--size', '512', '--write-agnostic'])\n",
    "print('\\ncompact set size:')\n",
    "subprocess.run(['du', '-sh', str(DATA_512)]) if shutil.which('du') else print(DATA_512)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "5dc85c85",
   "metadata": {},
   "outputs": [],
   "source": [
    "# GATE: eyeball a few overlays before training on this data.\n",
    "import json, matplotlib\n",
    "matplotlib.use('Agg')\n",
    "import matplotlib.pyplot as plt, matplotlib.patches as patches\n",
    "from PIL import Image\n",
    "\n",
    "d = json.load(open(DATA_512 / 'annotations' / 'tb_train.json'))\n",
    "by_img = {}\n",
    "for a in d['annotations']:\n",
    "    by_img.setdefault(a['image_id'], []).append(a)\n",
    "fig, axes = plt.subplots(1, 4, figsize=(16, 4))\n",
    "for ax, im in zip(axes, d['images'][:4]):\n",
    "    ax.imshow(Image.open(DATA_512 / 'images' / 'train' / im['file_name']), cmap='gray')\n",
    "    for a in by_img.get(im['id'], []):\n",
    "        x, y, w, h = a['bbox']\n",
    "        ax.add_patch(patches.Rectangle((x, y), w, h, fill=False, edgecolor='lime', lw=2))\n",
    "    ax.set_title(f\"{im['file_name'][:18]}  ({len(by_img.get(im['id'], []))} box)\"); ax.axis('off')\n",
    "plt.tight_layout()\n",
    "out = PROJECT / 'overlay_check.png'\n",
    "plt.savefig(out, dpi=70)\n",
    "from IPython.display import Image as IPImage, display\n",
    "display(IPImage(str(out)))\n",
    "print(f\"train images={len(d['images'])} boxes={len(d['annotations'])}\")"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "0cb88e0d",
   "metadata": {},
   "source": [
    "## Phase 4 — smoke test (a few batches, 1 epoch)\n",
    "Proves the whole train→eval path runs end-to-end. **This is the one to run on CPU** — it finishes\n",
    "in a minute or two and a near-zero AP is expected and fine."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "8d7c2494",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, sys\n",
    "subprocess.run([sys.executable, 'tools/tv_train.py',\n",
    "                '--work-dir', str(WORK/'smoke'), '--data-root', str(DATA_512)+'/', '--no-sas',\n",
    "                '--epochs', '1', '--batch-size', '2', '--limit-batches', '5',\n",
    "                '--drive-sync', str(RUNS/'smoke')])\n",
    "subprocess.run([sys.executable, 'tools/tv_eval.py',\n",
    "                '--ckpt', str(WORK/'smoke'/'epoch_1.pth'), '--data-root', str(DATA_512)+'/',\n",
    "                '--drive-sync', str(RUNS/'smoke')])"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "b188f9b7",
   "metadata": {},
   "source": [
    "## Phase 5 — RetinaNet baseline\n",
    "**GPU strongly recommended** (24 epochs). Re-run after any interruption; it auto-resumes within a\n",
    "session. On CPU this can take hours — skip unless you have CUDA."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "6f0b2b0e",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, sys\n",
    "subprocess.run([sys.executable, 'tools/tv_train.py',\n",
    "                '--work-dir', str(WORK/'baseline'), '--data-root', str(DATA_512)+'/', '--no-sas',\n",
    "                '--epochs', '24', '--batch-size', '8', '--lr', '0.005', '--seed', '0',\n",
    "                '--drive-sync', str(RUNS/'baseline')])\n",
    "subprocess.run([sys.executable, 'tools/tv_eval.py',\n",
    "                '--ckpt', str(WORK/'baseline'/'epoch_24.pth'), '--data-root', str(DATA_512)+'/',\n",
    "                '--drive-sync', str(RUNS/'baseline')])"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "697a4fb4",
   "metadata": {},
   "source": [
    "## Phase 7 — SymFormer w/ RetinaNet (the primary comparison)\n",
    "`--sync-weights` keeps one `best.pth` (~300 MB) under `tb_runs/` — locally that's just a copy in\n",
    "your workspace; drop the flag if you don't want the extra file. The AP numbers in the logs are\n",
    "what the claim rests on, not the weights."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "d30f1903",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, sys\n",
    "subprocess.run([sys.executable, 'tools/tv_train.py',\n",
    "                '--work-dir', str(WORK/'symformer'), '--data-root', str(DATA_512)+'/',\n",
    "                '--attention', 'symattention', '--pe', 'spe', '--stn', '--direction', 'r2l',\n",
    "                '--epochs', '24', '--batch-size', '8', '--lr', '0.005', '--seed', '0',\n",
    "                '--drive-sync', str(RUNS/'symformer'), '--sync-weights'])\n",
    "subprocess.run([sys.executable, 'tools/tv_eval.py',\n",
    "                '--ckpt', str(WORK/'symformer'/'epoch_24.pth'), '--data-root', str(DATA_512)+'/',\n",
    "                '--drive-sync', str(RUNS/'symformer')])\n",
    "# GATE (primary claim): SymFormer AP50 > baseline AP50. Record both in results.md."
   ]
  },
  {
   "cell_type": "markdown",
   "id": "ef46835f",
   "metadata": {},
   "source": [
    "## Phase 8 — Table 8 ablation (long; each cell auto-resumes within a session)\n",
    "Checkpoints go to `work_dirs/abl/<name>` and are removed after each is evaluated so disk stays\n",
    "bounded — this only ever touches files inside your workspace. Each run's `eval_log.jsonl` (a few\n",
    "KB) lands under `tb_runs/abl/` — that's the table."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "cde5a01a",
   "metadata": {},
   "outputs": [],
   "source": [
    "import subprocess, glob, os\n",
    "DELETE_CKPTS_AFTER_EVAL = True   # frees work_dirs (~300MB/cell). Only ever touches the workspace.\n",
    "EPOCHS = 24\n",
    "\n",
    "# (name, extra flags) — most-informative cells first, so a partial run is still meaningful.\n",
    "CELLS = [\n",
    "    ('none_none',                    ['--no-sas']),\n",
    "    ('vanilla_ape',                  ['--attention','vanilla','--pe','ape']),\n",
    "    ('symattention_ape',             ['--attention','symattention','--pe','ape']),\n",
    "    ('symattention_spe_stn_r2l',     ['--attention','symattention','--pe','spe','--stn','--direction','r2l']),\n",
    "    ('symattention_spe_nostn_r2l',   ['--attention','symattention','--pe','spe','--no-stn','--direction','r2l']),\n",
    "    ('symattention_spe_stn_l2r',     ['--attention','symattention','--pe','spe','--stn','--direction','l2r']),\n",
    "    ('vanilla_rpe',                  ['--attention','vanilla','--pe','rpe']),\n",
    "    ('symattention_rpe',             ['--attention','symattention','--pe','rpe']),\n",
    "    ('vanilla_spe_nostn_l2r',        ['--attention','vanilla','--pe','spe','--no-stn','--direction','l2r']),\n",
    "    ('vanilla_spe_nostn_r2l',        ['--attention','vanilla','--pe','spe','--no-stn','--direction','r2l']),\n",
    "    ('vanilla_spe_stn_l2r',          ['--attention','vanilla','--pe','spe','--stn','--direction','l2r']),\n",
    "    ('vanilla_spe_stn_r2l',          ['--attention','vanilla','--pe','spe','--stn','--direction','r2l']),\n",
    "    ('symattention_spe_nostn_l2r',   ['--attention','symattention','--pe','spe','--no-stn','--direction','l2r']),\n",
    "]\n",
    "\n",
    "for name, flags in CELLS:\n",
    "    wd  = WORK / 'abl' / name        # weights (removed after eval)\n",
    "    log = RUNS / 'abl' / name        # logs (kept)\n",
    "    print('='*70); print('TRAIN', name); print('='*70)\n",
    "    subprocess.run([sys.executable, 'tools/tv_train.py', '--work-dir', str(wd),\n",
    "                    '--data-root', str(DATA_512)+'/', '--epochs', str(EPOCHS),\n",
    "                    '--batch-size', '8', '--lr', '0.005', '--seed', '0',\n",
    "                    '--drive-sync', str(log)] + flags)\n",
    "    ck = wd / f'epoch_{EPOCHS}.pth'\n",
    "    if ck.is_file():\n",
    "        print('EVAL', name)\n",
    "        subprocess.run([sys.executable, 'tools/tv_eval.py', '--ckpt', str(ck),\n",
    "                        '--data-root', str(DATA_512)+'/', '--tag', name, '--drive-sync', str(log)])\n",
    "        if DELETE_CKPTS_AFTER_EVAL:\n",
    "            for p in wd.glob('*.pth'):\n",
    "                _guard(p).unlink()\n",
    "            print('removed checkpoints for', name)\n",
    "    else:\n",
    "        print('!! no checkpoint for', name, '— training did not finish; re-run this cell to resume')\n",
    "\n",
    "print('\\nlogs (expect KB):');   subprocess.run(['du','-sh',str(RUNS)]) if shutil.which('du') else None\n",
    "print('weights on disk:');       subprocess.run(['du','-sh',str(WORK)]) if shutil.which('du') else None"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "27132be0",
   "metadata": {},
   "outputs": [],
   "source": [
    "# Collect every eval_log.jsonl into one table for results.md\n",
    "import glob, json\n",
    "rows = []\n",
    "for f in sorted(RUNS.glob('**/eval_log.jsonl')):\n",
    "    for line in open(f):\n",
    "        rows.append(json.loads(line))\n",
    "print(f\"{'run':32s} {'AP50':>6s} {'AP':>6s}\")\n",
    "for r in rows:\n",
    "    print(f\"{r.get('tag',''):32s} {r['AP50']:6.1f} {r['AP']:6.1f}\")"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "8a403fc4",
   "metadata": {},
   "source": [
    "## Cleanup — safe & opt-in (replaces the Colab \"reclaim Drive\" cell)\n",
    "\n",
    "Locally there is no Drive Trash to worry about, so nothing here runs automatically. This reports\n",
    "disk usage and, **only if you flip `DRY_RUN = False`**, prunes checkpoints (`.pth`) from\n",
    "`work_dirs/`. Logs are always kept, and deletions are confined to your workspace."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "0ca8abf0",
   "metadata": {},
   "outputs": [],
   "source": [
    "DRY_RUN = True   # set to False to actually delete the checkpoints listed below\n",
    "\n",
    "import glob\n",
    "pths = list(WORK.glob('**/*.pth'))\n",
    "total = sum(p.stat().st_size for p in pths)\n",
    "print(f'{len(pths)} checkpoint(s) under {WORK}, {total/1e9:.2f} GB total:')\n",
    "for p in pths:\n",
    "    print(f'   {p.stat().st_size/1e6:7.0f} MB  {p}')\n",
    "\n",
    "if pths and not DRY_RUN:\n",
    "    for p in pths:\n",
    "        _guard(p).unlink()\n",
    "    print(f'\\nDeleted {len(pths)} checkpoint(s), reclaimed {total/1e9:.2f} GB.')\n",
    "elif pths:\n",
    "    print('\\n[dry run] nothing deleted. Set DRY_RUN = False to remove these.')\n",
    "\n",
    "logs = list(RUNS.glob('**/*.jsonl'))\n",
    "print(f'\\nKept {len(logs)} log file(s) (the results) — never auto-deleted:')\n",
    "for p in logs:\n",
    "    print(f'   {p.stat().st_size/1e3:7.1f} KB  {p}')"
   ]
  },
  {
   "cell_type": "markdown",
   "id": "510b980b",
   "metadata": {},
   "source": [
    "## Phase 10 — write-up\n",
    "Fill `results.md` (baseline vs SymFormer + the ablation table), update `CLAUDE.md` with the actual\n",
    "numbers, and note deviations (torchvision instead of mmdet, batch size, versions) and caveats\n",
    "(val-not-test; reduced data → trend not absolute numbers). Then commit and push."
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.x"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
